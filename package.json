{
  "name": "rotaract-madagascar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "db:setup": "node scripts/setup-rmg-clubs.mjs && node scripts/setup-rmg-program.mjs && node scripts/setup-rmg-users.mjs"
  },
  "dependencies": {
    "pocketbase": "^0.21.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.2.0",
    "typescript": "^5.7.3"
  }
}
